# Base-Backend-Repo
npm init -y; npm install express mongoose cors dotenv cookie-parser express-validator; npm install -D typescript ts-node @types/node @types/express @types/cors @types/cookie-parser @types/express-validator nodemon
